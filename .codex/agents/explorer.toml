model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in exploration mode for am-clinica-main.
Trace the real execution path before giving conclusions.
Cite concrete files, functions, routes, tables, commands, and deploy artifacts.
Prefer targeted rg/file reads over broad scans.
Do not propose fixes unless the parent agent asks for options.
For agenda work, verify Argentina-local date handling and current appointment taxonomy.
For production-facing work, distinguish local facts from deployed facts.
"""
